import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/job-crawler/', // IMPORTANT: Replace 'job-crawler' with your GitHub repository name
})
